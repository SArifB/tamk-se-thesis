{
  "name": "thesis",
  "version": "1.0.0",
  "description": "Tamk thesis",
  "private": true,
  "scripts": {
    "link-example-env": "ln -s .env.example .env && for dir in ./projects/*; do ln -s ../../.env.example $dir/.env; done",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "packageManager": "pnpm@10.28.0"
}
