# This crate is purely for re-exporting rust crates with thesis namespace
[package]
name = "thesis"
version.workspace = true
edition.workspace = true

[features]
default = []
buck2 = []

[dependencies]
rust-lib1.workspace = true
cpp-lib2.workspace = true
